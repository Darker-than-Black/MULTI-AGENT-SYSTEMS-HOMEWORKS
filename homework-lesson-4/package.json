{
  "name": "homework-lesson-4",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx src/main.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/main.js",
    "check": "tsc --noEmit",
    "invariant:check": "bash scripts/check-architecture-invariants.sh",
    "block:check:all": "bash scripts/run-all-block-validations.sh",
    "arch:check:staged": "bash scripts/check-architecture-sync.sh --staged",
    "arch:check:upstream": "bash scripts/check-architecture-sync.sh --upstream",
    "hooks:install": "bash scripts/install-hooks.sh"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@mozilla/readability": "^0.6.0",
    "dotenv": "^16.6.1",
    "jsdom": "^29.0.1",
    "openai": "^6.32.0"
  },
  "devDependencies": {
    "@types/jsdom": "^28.0.1",
    "@types/node": "^24.6.1",
    "tsx": "^4.20.6",
    "typescript": "^5.9.3"
  }
}
